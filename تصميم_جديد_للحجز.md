# تصميم جديد لصفحة الحجز 🎨

## الفكرة (من الصورة):

### التصميم الحالي:
- تاريخ واحد
- مواعيد في grid 2 أعمدة
- بسيط لكن مش واضح

### التصميم الجديد المقترح:
- **3 أيام في صف واحد** (اليوم، غداً، الثلاثاء)
- **أسهم للتنقل** (← →) لعرض أيام أخرى
- **كل يوم في عمود منفصل** مع:
  - اسم اليوم في header أزرق
  - المواعيد المتاحة تحته
  - زر "احجز" أحمر لكل موعد
  - "المزيد" لعرض مواعيد إضافية

---

## المميزات:

### 1. عرض أفضل ✅
- المريض يشوف 3 أيام مرة واحدة
- سهل المقارنة بين الأيام
- واضح ومنظم

### 2. تنقل سريع ✅
- أسهم لليمين واليسار
- عرض الأسبوع كامل
- سهل الوصول لأي يوم

### 3. تصميم احترافي ✅
- ألوان واضحة (أزرق للheader، أحمر للحجز)
- مساحات منظمة
- responsive للموبايل

---

## الكود المقترح:

```typescript
// State للأيام المعروضة
const [startDayIndex, setStartDayIndex] = useState(0);
const daysToShow = 3; // عدد الأيام المعروضة

// Generate next 14 days
const generateDays = () => {
  const days = [];
  for (let i = 0; i < 14; i++) {
    const date = new Date();
    date.setDate(date.getDate() + i);
    days.push({
      date: date.toISOString().split('T')[0],
      dayName: date.toLocaleDateString('ar-EG', { weekday: 'long' }),
      dayNumber: date.getDate(),
      slots: generateTimeSlots(doctor.workingHours, date)
    });
  }
  return days;
};

// Navigation
const handlePrevDays = () => {
  setStartDayIndex(Math.max(0, startDayIndex - daysToShow));
};

const handleNextDays = () => {
  setStartDayIndex(Math.min(days.length - daysToShow, startDayIndex + daysToShow));
};
```

---

## التصميم:

```jsx
<div className="bg-white rounded-2xl p-6">
  <h3 className="text-2xl font-bold text-center mb-6">
    اختـــار ميعاد الحجز
  </h3>
  
  <div className="flex items-center gap-4">
    {/* Previous Arrow */}
    <button 
      onClick={handlePrevDays}
      disabled={startDayIndex === 0}
      className="p-3 rounded-lg border hover:bg-gray-50"
    >
      ←
    </button>
    
    {/* Days Grid */}
    <div className="flex-1 grid grid-cols-3 gap-4">
      {visibleDays.map(day => (
        <div key={day.date} className="border rounded-lg overflow-hidden">
          {/* Header */}
          <div className="bg-blue-600 text-white text-center py-3">
            <div className="font-bold">{day.dayName}</div>
            <div className="text-sm">{day.dayNumber}/17</div>
          </div>
          
          {/* Time Slots */}
          <div className="p-3 space-y-2 max-h-64 overflow-y-auto">
            {day.slots.slice(0, 5).map(slot => (
              <div key={slot} className="text-center text-blue-600 font-semibold">
                {slot}
              </div>
            ))}
            
            {day.slots.length > 5 && (
              <button className="text-blue-600 text-sm">
                المزيد
              </button>
            )}
          </div>
          
          {/* Book Button */}
          <button className="w-full bg-red-600 text-white py-3 font-bold hover:bg-red-700">
            احجز
          </button>
        </div>
      ))}
    </div>
    
    {/* Next Arrow */}
    <button 
      onClick={handleNextDays}
      disabled={startDayIndex >= days.length - daysToShow}
      className="p-3 rounded-lg border hover:bg-gray-50"
    >
      →
    </button>
  </div>
</div>
```

---

## للموبايل:

```jsx
// عرض يوم واحد فقط على الموبايل
<div className="grid grid-cols-1 md:grid-cols-3 gap-4">
```

---

## عايز أنفذ التصميم ده؟

قولي "نفذ" وأنا هعمل الكود كامل! 🚀
