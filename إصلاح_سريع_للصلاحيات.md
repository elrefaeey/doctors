# 🚨 إصلاح سريع لخطأ الصلاحيات

## المشكلة
```
Error fetching pending requests: FirebaseError: Missing or insufficient permissions
```

## الحل السريع (دقيقة واحدة)

### الخيار 1: قواعد مبسطة (موصى به للتطوير)

1. **افتح Firebase Console**
   https://console.firebase.google.com/project/doctor-20c9d/firestore/rules

2. **احذف كل القواعد الموجودة**

3. **الصق هذا الكود:**

```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    
    function isSignedIn() {
      return request.auth != null;
    }
    
    match /users/{userId} {
      allow read: if isSignedIn();
      allow create: if isSignedIn();
      allow update: if isSignedIn() && request.auth.uid == userId;
      allow delete: if false;
    }
    
    match /doctors/{doctorId} {
      allow read: if true;
      allow write: if isSignedIn();
    }
    
    match /appointments/{appointmentId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }
    
    match /pendingRequests/{requestId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }
    
    match /subscriptionPlans/{planId} {
      allow read: if true;
      allow write: if isSignedIn();
    }
    
    match /specializations/{specId} {
      allow read: if true;
      allow write: if isSignedIn();
    }
    
    match /translations/{lang} {
      allow read: if true;
      allow write: if isSignedIn();
    }
    
    match /notifications/{notificationId} {
      allow read: if isSignedIn();
      allow write: if isSignedIn();
    }
    
    match /reviews/{reviewId} {
      allow read: if true;
      allow write: if isSignedIn();
    }
  }
}
```

4. **اضغط "Publish"**

5. **انتظر 10 ثواني**

6. **حدث الصفحة** (Ctrl+Shift+R)

### الخيار 2: قواعد للإنتاج (أكثر أماناً)

استخدم محتوى ملف `firestore.rules` في المشروع.

## ✅ التحقق

بعد تحديث القواعد:

### يجب أن تختفي هذه الأخطاء:
- ❌ Missing or insufficient permissions
- ❌ Error fetching pending requests
- ❌ Error fetching doctors

### يجب أن يعمل:
- ✅ تحميل الأطباء
- ✅ تحميل الطلبات المعلقة
- ✅ إضافة أطباء جدد
- ✅ حجز المواعيد

## 🔍 استكشاف الأخطاء

### لا تزال الأخطاء موجودة؟

1. **تأكد من نشر القواعد**
   - اضغط "Publish" في Firebase Console
   - انتظر 30 ثانية

2. **امسح الكاش**
   - Ctrl+Shift+R (Windows)
   - Cmd+Shift+R (Mac)

3. **تحقق من تسجيل الدخول**
   - تأكد أنك مسجل دخول
   - جرب تسجيل الخروج والدخول مرة أخرى

4. **تحقق من القواعد**
   - افتح Firebase Console → Firestore → Rules
   - تأكد أن القواعد متطابقة مع الأعلى

## 📝 ملاحظات

### القواعد المبسطة:
- ✅ سهلة التطبيق
- ✅ تعمل فوراً
- ✅ مناسبة للتطوير
- ⚠️ أقل أماناً للإنتاج

### القواعد الكاملة:
- ✅ أكثر أماناً
- ✅ تحكم دقيق بالصلاحيات
- ✅ مناسبة للإنتاج
- ⚠️ تحتاج إعداد دقيق

## 🎯 التوصية

**للتطوير والاختبار:**
استخدم القواعد المبسطة (الخيار 1)

**للإنتاج:**
استخدم القواعد الكاملة من `firestore.rules`

## 🔗 روابط سريعة

- **Firebase Console**: https://console.firebase.google.com/project/doctor-20c9d
- **Firestore Rules**: https://console.firebase.google.com/project/doctor-20c9d/firestore/rules
- **Authentication**: https://console.firebase.google.com/project/doctor-20c9d/authentication

---

## ملخص الخطوات

1. افتح Firebase Console → Firestore → Rules
2. الصق القواعد المبسطة
3. اضغط Publish
4. انتظر 10 ثواني
5. حدث الصفحة

**الوقت**: دقيقة واحدة
**النتيجة**: لا أخطاء! ✅
