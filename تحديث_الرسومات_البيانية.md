# تحديث الرسومات البيانية

## المشكلة
الرسومات البيانية في لوحة تحكم الدكتور كانت تعرض بيانات وهمية (hardcoded) بدلاً من البدء من صفر.

## الحل المطبق ✅

### تم تصفير البيانات
```typescript
// قبل التعديل
const visitsData = [
    { name: 'Mon', visits: 4 },
    { name: 'Tue', visits: 7 },
    // ...
];

const earningsData = [
    { name: 'Jan', amount: 4000 },
    { name: 'Feb', amount: 3000 },
    // ...
];

// بعد التعديل
const visitsData = [
    { name: 'Mon', visits: 0 },
    { name: 'Tue', visits: 0 },
    // ...
];

const earningsData = [
    { name: 'Jan', amount: 0 },
    { name: 'Feb', amount: 0 },
    // ...
];
```

## التطوير المستقبلي 🚀

### المرحلة التالية: ربط البيانات الحقيقية

#### 1. رسم الزيارات (Visits Chart)
يجب حساب عدد المواعيد لكل يوم من الأسبوع:

```typescript
const calculateVisitsData = (appointments: Appointment[]) => {
    const days = ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'];
    const visitsCount = days.map(day => ({
        name: day,
        visits: appointments.filter(apt => {
            const aptDay = new Date(apt.date).toLocaleDateString('en-US', { weekday: 'short' });
            return aptDay === day && apt.status === 'completed';
        }).length
    }));
    return visitsCount;
};
```

#### 2. رسم الأرباح (Earnings Chart)
يجب حساب الأرباح لكل شهر:

```typescript
const calculateEarningsData = (appointments: Appointment[], consultationPrice: number) => {
    const months = ['Jan', 'Feb', 'Mar', 'Apr', 'May', 'Jun', 'Jul', 'Aug', 'Sep', 'Oct', 'Nov', 'Dec'];
    const currentYear = new Date().getFullYear();
    
    const earningsCount = months.map((month, index) => ({
        name: month,
        amount: appointments.filter(apt => {
            const aptDate = new Date(apt.date);
            return aptDate.getMonth() === index && 
                   aptDate.getFullYear() === currentYear && 
                   apt.status === 'completed';
        }).length * consultationPrice
    }));
    
    return earningsCount;
};
```

#### 3. التطبيق في DoctorDashboard
```typescript
// في useEffect بعد جلب البيانات
useEffect(() => {
    if (appointments.length > 0 && doctorData) {
        const visits = calculateVisitsData(appointments);
        const earnings = calculateEarningsData(appointments, doctorData.consultationPrice);
        
        setVisitsData(visits);
        setEarningsData(earnings);
    }
}, [appointments, doctorData]);
```

## الملفات المعدلة

### `src/pages/DoctorDashboard.tsx`
- ✅ تصفير بيانات الرسومات
- 🔄 (مستقبلاً) إضافة دوال حساب البيانات الحقيقية

## الفوائد

### الحالية:
- ✅ الرسومات تبدأ من صفر (منطقي للموقع الجديد)
- ✅ لا توجد بيانات مضللة
- ✅ واضح للدكتور أنه لا توجد بيانات بعد

### المستقبلية:
- 📊 رسومات حقيقية من البيانات الفعلية
- 📈 تتبع الأداء بدقة
- 💰 حساب الأرباح الحقيقية
- 📅 تحليل الزيارات حسب الأيام

## ملاحظات

### البيانات الحالية:
- **Visits Chart**: يعرض أيام الأسبوع (Mon-Sun)
- **Earnings Chart**: يعرض الأشهر (Jan-Dec)
- **القيم**: جميعها صفر حالياً

### عند إضافة البيانات الحقيقية:
1. احسب من جدول `appointments`
2. فلتر حسب `status === 'completed'`
3. احسب الأرباح: `عدد المواعيد × سعر الكشف`
4. حدّث الرسومات تلقائياً

## الاختبار

### للتحقق من عمل الرسومات:
1. افتح لوحة تحكم الدكتور
2. اذهب إلى قسم "الإحصائيات"
3. يجب أن ترى:
   - رسم الزيارات بقيم صفر
   - رسم الأرباح بقيم صفر
   - خط مستقيم على الصفر

### عند إضافة مواعيد:
1. أضف مواعيد مكتملة
2. الرسومات ستبقى صفر (حتى يتم تطبيق المرحلة التالية)
3. في المستقبل: ستتحدث تلقائياً

## التاريخ
تم التصفير: 14 فبراير 2026
المرحلة التالية: ربط البيانات الحقيقية (قريباً)
